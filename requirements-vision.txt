# Local face / race analysis for scraper.mugshot_ethnicity
# Fully offline after first model download.
#
#   python -m pip install --user -r requirements-vision.txt
#   cd ../face-race && python -m pip install -e .
#
# Pin numpy below 2.3: newer wheels (2.4+) often break prebuilt
# tensorflow/keras/pandas binaries ("numpy.dtype size changed").
numpy>=1.26.0,<2.3

# Primary race model — standalone FairFace package (shared with SORPA)
# Install from sibling repo:  cd ../face-race && pip install -e .
# Weights: ~/.face_race/weights/  (auto-download on first score)

# Legacy / fallback — DeepFace race head
deepface>=0.0.93
tensorflow>=2.15.0
tf-keras>=2.15.0
opencv-python>=4.8.0
pillow>=10.0.0
